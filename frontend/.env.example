# API base URL (Strategy A — baked in at build time on Railway)
# Local npm run dev: leave unset — Vite proxies /api → http://localhost:8080
# Local Docker Compose: http://localhost:8080
# Railway web service (build): https://<api>.up.railway.app
VITE_API_URL=http://localhost:8080
